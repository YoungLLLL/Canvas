# Canvium Gallery Showcase v1：阶段 5 中间验证记录

> 验收日期：2026-07-21  
> 范围：芝加哥艺术博物馆绘画馆藏、馆藏查询、作品详情、图片与权利状态

## 1. 当前结论

阶段 5 **尚未完成验收**。已实现的工程范围包括：

- ARTIC 原始响应的运行时校验、规范化和独立权利准入；
- 服务端馆藏与作品代理、5 分钟新鲜缓存、一次瞬时失败重试和 1 小时陈旧回退；
- 绘画、公共领域、权利无冲突、图片可用性等不可绕过的远端查询约束；
- 搜索、艺术家 OR 筛选、年代区间、图片可用性、排序和显式分页；
- 固定顺序的规范化查询 URL、默认值清理以及 10,000 条搜索窗口保护；
- 馆藏、作品卡和作品详情从阶段 4 占位界面迁移到既有视觉基线；
- 以 ARTIC 作品 ID（Wikidata `P4610`）批量精确匹配 Wikimedia Commons 图片；
- Commons 公共领域许可二次校验、响应式缩略图、LQIP 渐进预览和单图失败回退；
- 作品缩放、拖动、复位、键盘控制和全屏入口；
- 馆藏返回时的 URL、滚动位置和作品卡焦点恢复；
- 中英文切换时保留当前资源和馆藏查询；
- 初始加载、合法空结果、无效年代、上游错误、陈旧数据和单图失败的不同状态；
- 馆藏与作品页 canonical、语言替代链接、标题、描述和有权图片分享信息。

## 2. 真实数据抽查

2026-07-21 使用 ARTIC 实时响应完成以下抽查：

| 查询／样本       | 结果                                                                                                               |
| ---------------- | ------------------------------------------------------------------------------------------------------------------ |
| 默认公开绘画候选 | 当前远端匹配 `1,865` 条                                                                                            |
| `monet` 全文搜索 | 修正为硬过滤后匹配 `44` 条；不再把全文词只作为排序信号                                                             |
| 仅资料绘画       | 当前远端匹配 `81` 条                                                                                               |
| 分页样本         | 第 1、2、25、80 页返回不同作品集合，覆盖接近前 1,000 条的深页抽查                                                  |
| 权利抽查         | 已返回样本均为 `Painting`、`public_domain`、无非空作品版权声明                                                     |
| Commons 映射抽查 | 两批各 100 件 ARTIC 合格记录中分别有 `97`、`98` 件存在 Commons 图片映射                                            |
| Commons 许可抽查 | 100 件 ARTIC 合格记录映射出 106 个图片版本，其中 105 个明确为公共领域或 CC0；最终仍以 ARTIC 权利字段为作品准入依据 |
| 正式页面图片     | 默认页 10/10、第二页 11/11、`monet` 搜索 11/11 图片加载成功，无失败占位，均来自 `upload.wikimedia.org`             |
| 响应时间         | 抽查中的未缓存馆藏请求约 `0.5–3.8s`；缓存命中约 `0.08s`                                                            |

复杂搜索已经从会忽略硬过滤的顶层 `q + query` 组合，改为同一 Elasticsearch `bool.must`
中的 `multi_match`。分页使用搜索端点明确支持的 `from/size`，并对最后一个可访问页缩小
`size`，避免请求越过 10,000 条窗口。

高频人工抽查曾触发馆方匿名限流并返回 403。正式浏览器测试已改为单线程，服务端同时提供
短期缓存、一次重试、陈旧回退和明确错误画面，不把上游失败误写成空结果或作品撤下。

## 3. 图片交付路径

当前执行环境访问 ARTIC `www.artic.edu/iiif/2` JPEG 端点会进入 Cloudflare JavaScript
挑战；`info.json`、ARTIC API 和 IIIF Manifest 可访问，但 Manifest 最终仍指向同一受挑战的
JPEG 地址。普通 `<img>` 无法完成顶层网页挑战，因此正式页面不再把 ARTIC IIIF 作为图片
交付路径。

作品与权利状态继续以 ARTIC 为权威来源。图片通过 Wikidata 的 ARTIC artwork ID 属性
`P4610` 精确映射到 Wikimedia Commons，不使用标题模糊匹配；映射后再次读取 Commons
机器可读许可，接受 Public Domain、CC0 及许可信息明确的 CC BY／BY-SA／BY-NC／BY-NC-SA／BY-ND／BY-NC-ND 图片。NC 图片仅限当前非商业项目；ND 图片只允许技术性转换，不允许内容性修改。ARTIC 与 Wikidata 权利状态冲突时以 ARTIC
为准。没有可信映射的记录降级为仅资料状态，不再请求已知会失败的 ARTIC JPEG。

Wikimedia 映射与权利元数据按 ARTIC ID 持久化到本地缓存，默认位于
`apps/web/.cache/wikimedia/artic`；其中不保存图片文件。成功映射缓存 30 天，确认无可用
映射的结果缓存 1 天。缓存未命中时才访问 Wikidata 与 Commons，所有请求共用限速队列；
遇到 429 时遵循 `Retry-After` 并执行指数退避。限流和临时网络错误分别标记为
`commons_rate_limited`、`commons_temporarily_unavailable`，不再误记为确认缺图。

## 4. 自动验证

- Prettier：通过；
- ESLint：通过；
- Next.js 路由类型与 TypeScript：通过；
- Vitest：6 个测试文件、21 个测试通过；
- Next.js 生产构建：通过；
- Playwright Chromium：6 条桌面与移动核心路径、真实筛选、仅资料记录、查看器和返回恢复通过。
- 定向真实浏览器验证：默认馆藏、第二页、`monet` 搜索和作品详情均显示 Commons 图片，
  `naturalWidth > 0`，无 ARTIC IIIF 图片请求和失败占位。

浏览器覆盖的关键路径包括：

1. `HOME → MUS → COL → ART`；
2. 搜索／筛选提交后的 canonical URL；
3. 查询状态随语言切换保留；
4. `ART → COL` 恢复 URL、滚动和焦点；
5. 仅资料作品不伪造图片，并仍提供来源与权利说明；
6. 移动端可搜索、筛选、浏览和翻页，且不存在水平溢出；
7. 查看器放大与复位；
8. 单图失败使用真实 LQIP 或明确的无图回退。

## 5. 可重复批量验收工具

2026-07-22 新增根目录命令 `npm run verify:stage5`。该工具自动启动正式 Next.js 应用，并通过正式 `/api/catalog` 接口抽查数据，因此不会复制或绕过生产环境中的 ARTIC 权利准入、Wikidata `P4610` 映射和 Commons 许可校验。

约 500 件样本使用：

```bash
npm run verify:stage5 -- --pages 42 --concurrency 1 --page-delay-ms 500 --image-delay-ms 750
```

工具输出带版本号的 JSON 报告，统计映射率、权利与来源完整率、重复记录、分页与缓存耗时，并从全部可展示记录中均匀选择少量 Commons 图片做低流量可达性探测。429 与超时单列为探测受限，不误报为图片失效。详细参数见 `evaluation/README.md`。

首次 2 页 smoke 抽查覆盖 24 件最新更新的绘画候选：

- 24 件记录与权利／来源字段均通过，未发现重复记录；
- 21/24 件存在合规 Commons 映射，映射率为 87.5%，未达到 95% 门槛；
- 一张均匀抽样图片探测返回 HTTP 200；
- 页面请求为 248ms 与 3,894ms，随后同页缓存复查为 12ms；
- 当前缺口为 `artic:65940`、`artic:90207` 与 `artic:25781`。

该 smoke 样本不足以替代 42 页长跑，但已经证明 Stage 5 不能关闭：最新更新记录的映射缺口超过当前门槛。随后对三件缺口做了 Wikidata 与 Commons 机器可读字段复查：`artic:65940` 和 `artic:90207` 均有 `P4610`，但没有 `P18`；`artic:25781` 有 `P18`，但对应 Commons 文件为 `CC BY-SA 4.0` 且标记为受版权保护，不符合当前只接受 Public Domain／CC0 的图片规则。三件降级均符合现行准入逻辑，不是映射程序误判。下一步需要决定是接受这类作品以仅资料状态出现，还是研究不降低权利标准的第二个精确图片来源，然后再执行约 500 件的正式抽查。

2026-07-27 已完成 42 页正式长跑，报告为
`evaluation/runs/stage5-catalog-2026-07-27T04-33-05Z.json`：

- 共返回 504 条记录、488 个唯一作品，出现 16 条跨页重复；
- 490 条记录属于 Commons 映射候选，其中 150 条有合规图片，映射率为 30.6%，远低于 95% 门槛；
- 权利与来源字段完整率为 100%，42 页均成功返回，没有页面请求失败；
- 5 张均匀抽样图片中 4 张得到结论且均可访问；1 张被 Wikimedia 返回 429，因此图片可达率为 100%，有效探测覆盖率为 80%；
- 大量连续页面为 0/12 映射，缺口不是少数异常作品，Stage 5 仍不能关闭。

本次长跑同时说明 `updated_at desc, id asc` 的显式排序仍可能在跨页期间得到重复记录，需要继续核对上游搜索分页稳定性或改用快照／本地目录；在解决映射覆盖与分页稳定性前，不应把实时搜索结果描述为稳定完整馆藏。
